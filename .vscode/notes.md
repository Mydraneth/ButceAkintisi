Final Workflow Summary
To run your app: Use fvm flutter run (instead of just flutter run).

To install packages: Use fvm flutter pub get.

To upgrade: Change the version on Computer 1 (fvm use 3.25.0), commit the change. On Computer 2, just pull and run fvm install.